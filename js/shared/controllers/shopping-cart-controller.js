(function() {
    'use strict';
    // the shopping cart controller
    angular.module('shoppingKartApp.shared.controllers').controller('ShoppingCartController', ['$scope', '$routeParams', 'GlobalDataService',
        function($scope, $routeParams, DataService) {
            
           
        }
    ]);
})();